# Dominium

WIP

Ideas:

Alias /dominium to /do (possibility to disable)

- /do (enable|stop|disable)
- /do muffle (on|toggle|off) - muffled chat, defaults to on
- /do blind (on|toggle|percentage|off) - black overlay with opacity, defaults to on
- /do mute (on|toggle|off) - disable chat input, defaults to on
- /do deaf (on|toggle|off) - hide chat messages, defaults to on
- /do drunk (on|toggle|off) - screen effect overlay, defaults to on
- /do bound (on|toggle|off) - disable move controls, defaults to on
- /do follow target (on|toggle|off) - force follow, defaults to on (quote support)
- /do confine (on|toggle|off) - can't leave room/house, defaults to on
- /do fpv (on|toggle|off) - force first person view, defaults to on

Safe keybind by default on END (configuration window)

- Puppet master like with ACL (token) where can send base64 gzipped "command list" with id/names/alias to share (copy-paste manual operation) or generated file - Token gives permission to specific commands to a specific person (character name/world) for specific duration or until invalidated
- Custom syntax like <do.3> or alias (button to automatically add in chat or copy in clipboard)
